[package]
name = "oxiplate-test-slow-escape-ints"
edition = "2024"
publish = false

[dev-dependencies]
trybuild = "1.0.115"
oxiplate = { path = "../../../", default-features = false, features = ["built-in-escapers", "debug-fast-escape-type-priority"] }
