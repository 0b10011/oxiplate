[package]
name = "oxiplate-test-slow-escape-ints"
publish = false
license.workspace = true
edition.workspace = true

[dev-dependencies]
trybuild = "1.0.116"
oxiplate = { path = "../../../", default-features = false, features = ["built-in-escapers", "_debug-fast-escape-type-priority"] }
