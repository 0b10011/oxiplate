[package]
name = "oxiplate-derive-test-unreachable-stable"
edition = "2021"
publish = false
version = "0.1.0"

[dev-dependencies]
trybuild = "1.0.115"
oxiplate-derive = { path = "../../../", features = ["unreachable"] }
