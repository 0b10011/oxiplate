[package]
name = "oxiplate-test-file-extension-inferrence-off"
edition = "2024"
publish = false

[dev-dependencies]
trybuild = "1.0.101"
oxiplate = { path = "../../../", features = ["external-template-spans"] }
