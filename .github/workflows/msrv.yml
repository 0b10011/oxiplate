name: msrv

on:
  workflow_call:
    inputs:
      cache-path:
        description: "Path to restore from cache"
        type: string
      cache-key:
        description: "Key to lookup cache item"
        type: string
    outputs:
      msrv:
        description: "Minimum stable Rust version for Oxiplate"
        value: ${{ jobs.msrv.outputs.msrv }}

jobs:
  msrv:
    runs-on: ubuntu-latest
    outputs:
      msrv: ${{ steps.msrv.outputs.msrv }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Restore setup cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v5
        with:
          path: ${{ inputs.cache-path }}
          key: ${{ inputs.cache-key }}
          fail-on-cache-miss: true

      - name: Get `rust-version` from `/Cargo.toml`
        id: msrv
        run: |
          MSRV="$(just get-msrv)"

          SAFE_REGEX='^[0-9]+\.[0-9]+\.[0-9]+$'
          if [[ ! $MSRV =~ $SAFE_REGEX ]]; then
            echo '`rust-version` value in "/Cargo.toml" is not in `#.#.#` format'
            exit 1
          fi

          echo "msrv=$MSRV" >> $GITHUB_OUTPUT
