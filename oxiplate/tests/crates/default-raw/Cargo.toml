[package]
name = "oxiplate-test-default-raw"
edition = "2021"
publish = false

[dev-dependencies]
trybuild = "1.0.115"
oxiplate = { path = "../../../", default-features = false, features = ["config"]}
